import{ FC } from "react";


const Navbar:FC = () => {
  return (
    <ul className="navbar">
      <a href="#">Home</a>
      <a href="#">Shop Collection</a>
      <a href="#">Our Story</a>
      <a href="#">Contact</a>
    </ul>
  );
};

export default Navbar;
